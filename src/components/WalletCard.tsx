import type {ChainInfo} from "../lib/chains";type Props={address:string;chain:ChainInfo|null;chainId:number;balance:string|null;onRefresh:()=>void;busy:boolean};export default function WalletCard({address,chain,chainId,balance,onRefresh,busy}:Props){return <section className="wallet-card"><div className="card-top"><div><span className="eyebrow">CONNECTED WALLET</span><h2>{address}</h2></div><span className="status-dot">● Live</span></div><div className="metrics"><div className="metric"><span>Network</span><strong>{chain?.name??`Chain ${chainId}`}</strong></div><div className="metric"><span>Native balance</span><strong>{balance??"—"} {chain?.nativeSymbol??""}</strong></div></div><div className="card-footer"><span>Read-only wallet information</span><button className="ghost-button" onClick={onRefresh} disabled={busy}>{busy?"Refreshing…":"Refresh"}</button></div></section>}