type Props={connected:boolean;busy:boolean;address:string|null;onConnect:()=>void;onDisconnect:()=>void};export default function ConnectButton({connected,busy,address,onConnect,onDisconnect}:Props){return connected?<div className="wallet-actions"><span className="address-pill">{address}</span><button className="secondary-button" onClick={onDisconnect}>Disconnect</button></div>:<button className="primary-button" onClick={onConnect} disabled={busy}>{busy?"Connecting…":"Connect Wallet"}</button>}